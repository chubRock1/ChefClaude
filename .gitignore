# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Local env / tooling (secrets live in Vercel, never in the repo)
.env
.env.*
.vercel/
node_modules/

# Nutrition enrichment tool — local API cache + raw recipe exports (never committed)
tools/.fdc_cache.json
tools/recipes_src/

# Python build cruft
__pycache__/
*.pyc

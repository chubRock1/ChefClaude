# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Local env / tooling (secrets live in Vercel, never in the repo)
.env
.env.*
.vercel/
node_modules/

# Nutrition enrichment tool — local API cache + raw recipe exports (never committed)
tools/.fdc_cache.json
tools/recipes_src/

# Python build cruft
__pycache__/
*.pyc

# enrichment run log
tools/run.log

# Web-verify: keep the full personal recipe DB out of the public repo
tools/web_verify/chef_claude_recipe_database.json
